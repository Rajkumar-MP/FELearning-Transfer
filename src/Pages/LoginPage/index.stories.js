import { html } from 'lit-html';
import './LoginPage.js';

export default {
  title: 'Page',
};

function Template() {
  return html` <login-page> </login-page> `;
}

export const Landing = Template.bind({});
